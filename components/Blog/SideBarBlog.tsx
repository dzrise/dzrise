import { ICategory } from "../../types/category";


interface IProps {
        categories: ICategory[]
}
const SideBarBlog: React.FC<IProps> = (props: IProps) => {
    return (
        <aside>

        </aside>
    )
}
export default SideBarBlog;
